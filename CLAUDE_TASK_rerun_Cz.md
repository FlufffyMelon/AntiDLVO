# Задача для Claude Code: досчитать 7 расчётов свипа по C_z (нейтральная система)

Работаешь на кластере softcluster в `~/AntiDLVO`. Это MC код (Python + CuPy) для смеси ионов и цвиттерионов (жёсткие диполи) в щелевой поре с явными заряженными стенками. Запуски идут через `run.sh` + OmegaConf конфиг, пачки через sbatch скрипты.

Папка синхронизируется с ноутбуком автора через mutagen (two-way-safe, `results*/`, `*.xyz`, `*.npz`, `.venv/` и `.git` исключены). Правки кода, которые ты сделаешь, уедут на ноутбук. Это нормально.

## Контекст

Продакшн свип нейтральной системы (с противоионами) лежит в `results_prod_neutral/pz_{17,27p6}/{C,Cz,H,sigma}/`. Его запускали двумя задачами `sbatch_grid_ions_dipole.sh` (PZ=17 и PZ=27.6) 16.02.2026, лог в `slurm-51959.out` и `slurm-51960.out`. Обе задачи убиты по TIME LIMIT (24 ч) 17.02 около 12:55, поэтому свип по C_z недоделан.

Что уже есть и что нужно:

| p_z | есть в Cz | нужно досчитать |
|---|---|---|
| 17 D | C_z = 0, 0.1 | C_z = 0.3, 0.5, 0.7 |
| 27.6 D | C_z = 0 | C_z = 0.1, 0.3, 0.5, 0.7 |

Остальные параметры у всех семи расчётов базовые и совпадают с уже посчитанными: H = 3 нм, σ = −20 мкКл/см², C = 0.01 М, L = 20 нм, n_steps = 1e6, log/xyz interval = 50.

Точка p_z = 27.6 D, C_z = 0.1 одновременно является базовой точкой свипов H (H=3), sigma (σ=−20) и C (C=0.01). Для 27.6 D её сейчас нет ни в одном свипе.

**Ничего другого не пересчитывать.** Недобитые расчёты (19176, 18514 и 19981 строк в `simulation_data.csv` из 20000) остаются как есть, они пригодны для анализа. Дыры σ = −25, −30 и C = 0.07, 0.1 не закрываем.

## Почему расчёты не досчитались (важно, это надо починить до запуска)

В `slurm-51959.out`/`slurm-51960.out` видны прогресс-бары `Count: 101/505 [4:23:27<126:04:13 ...]`, `Count: 26/72 [14:10:05<85:45:06]` и т.п. Это стадия **начальной случайной расстановки частиц**, а не MC. Числа 72/216/361/505 соответствуют числу диполей при C_z = 0.1/0.3/0.5/0.7. Расстановка шла десятки часов и съела лимит времени. Автор помнит это как «система не собирается при маленьких порах и больших концентрациях». На деле причина в коде `_init_random_configuration` в `src/utils.py`.

1. **Бесконечный цикл.** `attempts += 1` стоит после цикла `while not placed and attempts < max_attempts`, а не внутри него. Счётчик попыток не растёт, и `max_attempts` никогда не срабатывает. Если позиция не находится, цикл крутится вечно.
2. **Один общий `min_distance` для всех пар.** В конфиге `min_distance = 2*max(r_Na, r_Cl, r_Ip, r_Im, r_G) + 1e-6 = 0.766` нм, и он применяется ко всем парам атомов, включая Na–Na (контакт 0.662), и к атомам стенок Wb/Wt. Это сильно завышено.
3. **Общий `padding` по z = 0.766 нм.** При H = 3 для центров остаётся слой всего 1.47 нм вместо, например, [r_Na, H − r_Na] = 2.34 нм для Na. В этот слой надо впихнуть 968 противоионов + 7 пар соли + до 505 диполей. Случайная последовательная вставка (RSA) заклинивает около своего предела упаковки (~0.38), а здесь эффективная упаковка оказывается порядка этого предела.
4. **Порядок вставки.** Сначала ставятся 975 Na (за секунды), потом Cl, потом диполи в уже забитый объём. Диполи самые громоздкие, их надо ставить первыми.

Оценка по объёму показывает, что физически система собирается. Суммарная доля объёма твёрдых сфер при p_z = 27.6 D, C_z = 0.7, H = 3 составляет около 0.28 от объёма поры, это плотная, но жидкая система (кристаллизация HS около 0.49). Проблема только в алгоритме начальной расстановки.

## План

### Шаг 0. Осмотреться (ничего не меняя)

- `git status`, `git log --oneline | head` на кластере. Перед правками сделай коммит текущего состояния или хотя бы `cp src/utils.py src/utils.py.bak_before_init_fix`.
- `sinfo`, `squeue -u $USER`, `scontrol show partition` (лимит времени), `nvidia-smi` на целевом узле. Раньше использовался `-w node4`, `--gpus 1`, 22 расчёта параллельно на одной GPU при ~25 it/s на расчёт. Несколько расчётов на одной GPU это штатный режим. Если на узле две GPU, можно раскидать через `--ewald.gpu_device 0/1` (в `run_calc` это закомментировано).
- Посмотри хвосты `slurm-51959.out`, `slurm-51960.out` и остальных `slurm-*.out`, чтобы убедиться, что картина та же, что описана выше.
- **Сверь конфиг.** Возьми `config_backup.yaml` из уже досчитанного нейтрального расчёта, например `results_prod_neutral/pz_17/Cz/*Cz_0_*/config_backup.yaml`, и сравни его с текущим `configs_prod/ions_dipole.yaml` (с учётом overrides из sbatch). Новые расчёты должны отличаться от старых **только** значениями `pz` и `Cz`. Особенно проверь `ewald` (eps 1e-8, real_cut 1.49, dielectric 78, z_scale_factor 4, dipole_correction true), `n_steps: 1000000`, `max_displacement`, `max_rotation`, `log_interval`/`xyz_interval: 50`, `ghost_count: 1`, радиусы, число Na = C·V + 2·N_wall. Если что-то расходится, остановись и сообщи автору.
- Проверь, что `sbatch_grid_ions_dipole.sh` пишет в `results_prod_neutral/...` и что имена папок имеют вид `ions_dipole_Cz_pz_<pz>_H_3_sigma_-20_C_0.01_Cz_<Cz>_<timestamp>`.

### Шаг 1. Починить начальную расстановку

Меняется **только** начальная конфигурация, гамильтониан и MC не трогаем. Равновесные результаты от этого не зависят, если расчёт уравновешен. В анализе первые 2000 кадров (10%) и так отбрасываются.

Требования к исправленному `_init_random_configuration` (или новому режиму, например `mode: "random_hs"`, чтобы старое поведение осталось доступно):

- `attempts += 1` внутри `while`. Если молекулу не удалось поставить за `max_attempts`, **падать с исключением**, а не печатать `Failed to place` и молча продолжать с меньшим числом частиц.
- Проверка перекрытий **по парам типов** с порогом r_i + r_j (+ небольшой запас, например 1e-3 нм), где радиусы берутся из `hard_sphere` в `pairs` (или из `r_*` в конфиге). Для пар без hard_sphere (например, ион со стенкой Wb/Wt) ограничение по расстоянию не нужно, там работает hard_wall. Учти `exclude_intermol` (внутримолекулярные пары не проверяются).
- Ограничение по z **для каждого атома** молекулы свой: z ∈ [r_wall_i, H − r_wall_i] по `one_body.hard_wall` данного типа (включая концы диполя Ip/Im и ghost G). Центр диполя выбирать так, чтобы все его атомы попали в допустимые слои.
- Порядок вставки: сначала диполи (самые громоздкие), потом Cl, потом Na.
- Для скорости достаточно векторной проверки по numpy как сейчас. Если на C_z = 0.7 всё равно медленно, добавь простой cell list или сетку ячеек.
- Никаких изменений в `sampler.py`, `topology.py`, `ewald_handler.py`.

### Шаг 2. Быстрая проверка перед продакшеном

Самый тяжёлый случай: p_z = 27.6, C_z = 0.7, H = 3, σ = −20, C = 0.01. Запусти его коротко (например, `--simulation.n_steps 2000`, results_dir `results_tmp/init_test`) и проверь:

- время расстановки (цель в пределах минут);
- числа частиц: Na = 975, Cl = 7, диполей 505, атомов стенки 484 + 484;
- ни одна пара с hard_sphere не ближе r_i + r_j, все атомы в своих z-слоях;
- начальная энергия конечная (не inf/nan), MC стартует;
- скорость MC в it/s. По ней оцени время 1e6 шагов при 7 параллельных расчётах. Раньше было ~25 it/s на расчёт при 22 параллельно (~11 ч на расчёт), но при C_z = 0.7 атомов больше и может быть медленнее.

Если оценка времени близка к лимиту партиции, не урезай n_steps. Лучше разнести расчёты на две задачи или две GPU либо запросить больший `--time`, если партиция позволяет. Если ничего из этого не выходит, сообщи автору.

**Опционально (желательно):** прогнать один уже посчитанный случай (p_z = 17, C_z = 0.1) с новой расстановкой на ~200k шагов и сравнить среднюю энергию и профиль Na после 100k шагов с существующим расчётом. Так мы покажем, что смена начальной конфигурации не влияет на результат. Делать в той же задаче восьмым лёгким расчётом в `results_tmp/`, не в `results_prod_neutral`.

### Шаг 3. Запуск

Сделай отдельный скрипт `sbatch_rerun_Cz_neutral.sh` на основе `sbatch_grid_ions_dipole.sh` (переиспользуй `run_calc`, `bg_limit`, тайминги), но с явным списком ровно из 7 расчётов. Каждый вызывается как в оригинале:

```bash
./run.sh configs_prod/ions_dipole.yaml \
  --pz <17|27.6> --H 3 --sigma -20 --C 0.01 --Cz <Cz> \
  --experiment.name "ions_dipole_Cz_pz_<17|27p6>_H_3_sigma_-20_C_0.01_Cz_<Cz>" \
  --experiment.results_dir "results_prod_neutral/pz_<17|27p6>/Cz"
```

Список:

| p_z | C_z |
|---|---|
| 17 | 0.3 |
| 17 | 0.5 |
| 17 | 0.7 |
| 27.6 | 0.1 |
| 27.6 | 0.3 |
| 27.6 | 0.5 |
| 27.6 | 0.7 |

Прочее:
- Один sbatch на все 7 (все параллельно, `MAX_PARALLEL=7` или 8 с опциональным тестом) на одном узле с GPU, если по оценке из шага 2 укладываемся в лимит.
- Явно пропиши `#SBATCH --time`, `--job-name=zw_Cz_rerun`, узел выбери по `sinfo` (раньше был `node4`).
- Не меняй дефолтный `CONFIG_FILE` и не трогай `sbatch_grid_ions_dipole.sh`.
- После сабмита проверь через несколько минут, что все 7 процессов прошли расстановку и крутят MC (в slurm логе появился `MC:` прогресс-бар у каждого).

### Шаг 4. После завершения

- Для каждого из 7 расчётов проверь, что есть `trajectory.xyz`, `config_backup.yaml`, `simulation_data.csv` с 20000 строками данных и в логе стоит `Simulation completed successfully!`.
- Проверь уравновешенность: энергия (колонка энергии в `simulation_data.csv`) выходит на плато раньше 2000-й записи (т.е. раньше 100k шагов). Если нет, сообщи, сколько записей нужно отбросить.
- Базовая точка p_z = 27.6, C_z = 0.1 должна быть видна и в свипах H, sigma и C. Заведи на её папку симлинки в `results_prod_neutral/pz_27p6/H/`, `.../sigma/`, `.../C/` с именами по той же схеме (`ions_dipole_H_pz_27p6_H_3_...`, `ions_dipole_sigma_pz_27p6_..._sigma_-20_...`, `ions_dipole_C_pz_27p6_..._C_0.01_...`), чтобы ноутбук `prod_visual.ipynb` подхватил её без правок. Симлинки, не копии (траектории большие).

### Шаг 5. Отчёт

Запиши в `RERUN_Cz_REPORT.md` в корне репозитория:
- что было исправлено в расстановке (diff или краткое описание), где лежит бэкап;
- результаты проверки из шага 2 (время расстановки, it/s, числа частиц);
- id задачи slurm, узел, фактическое время каждого расчёта;
- статус каждого из 7 расчётов и путь к папке;
- итог проверки уравновешенности;
- результат опционального сравнения старой и новой расстановки, если делал;
- всё необычное, что заметил.

## Чего не делать

- Не удалять и не перезаписывать ничего в `results_prod`, `results_prod_neutral` и других `results*`.
- Не менять физику: потенциалы, Ewald, сэмплер, n_steps, радиусы, заряды.
- Не ставить расчёты сверх этих 7 (и опционального тестового).
- Не трогать `mutagen.yml`.
