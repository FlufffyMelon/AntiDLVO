# Задача для Claude Code: аудит точности Эвальда во всех продакшн расчётах

Работаешь на кластере softcluster в `~/AntiDLVO`. Это MC код (Python + CuPy) для ионов и цвиттерионов (жёсткие диполи Ip–Im с ghost G) в щелевой поре с явными заряженными стенками (атомы Wb при z = 0 и Wt при z = H, заряд −1e на узлах квадратной решётки). Электростатика считается через Ewald3D с растяжением ячейки по z (`z_scale_factor`), поправкой на дипольный момент слэба и поправкой для ненейтральных систем (dos Santos, Girotto, Levin 2016). Папка синхронизируется с ноутбуком автора через mutagen (`results*/`, `*.xyz`, `*.npz`, `.venv/`, `.git` исключены).

**Цель.** Количественно проверить, насколько точно посчитана электростатика во всех продакшн расчётах, и дать однозначный вердикт по каждой группе расчётов. Возможных вердиктов три: «годится», «годится с оговорками», «пересчитывать». Если пересчитывать, нужно предложить корректные параметры и оценить стоимость.

## Ограничения

- **Не останавливать и не трогать** задачи, которые сейчас идут в slurm (7 расчётов свипа C_z, см. `CLAUDE_TASK_rerun_Cz.md` и `RERUN_Cz_REPORT.md`, если он уже есть). Если по итогам аудита их стоит снять, напиши это в отчёте, но сам не снимай.
- Не менять продакшн код в `src/` и конфиги в `configs_prod/`. Вся работа идёт в новой папке `ewald_audit/` (скрипты, результаты, графики).
- Ничего не удалять и не перезаписывать в `results*`.
- Тяжёлые вычисления запускать на вычислительном узле через slurm (`sbatch`/`srun`), а не на логин-узле. GPU делим с идущими расчётами, поэтому свои задачи держи короткими и скромными по памяти.

## Что уже известно (предварительный анализ, требует проверки)

1. В `configs_prod/ions_dipole.yaml`:
   ```yaml
   ewald:
     eps: 1e-8
     real_cut: 1.49
     alpha: ${eval:"sqrt(-log(${ewald.eps})) / ${ewald.real_cut}"}          # = 2.88
     n_c: ${eval:"ceil(2 * sqrt(${ewald.alpha} * (-log(${ewald.eps}))) * 3.0 / (2 * pi))"}   # = 7
     z_scale_factor: 4.0
   ```
   В `EwaldHandler.initialize_k_vectors` строится **одинаковая** целочисленная сетка `-n_c..n_c` по всем трём осям, где b_i = 2π/L_i и L_z берётся с учётом z_scale. Коробка 20 × 20 × (4H) нм. Длина в формуле для n_c захардкожена как 3.0 нм, а не взята от реальных сторон. Нужный по той же формуле k_max ≈ 14.6 нм⁻¹, а фактический около 2.2 нм⁻¹ по x и y, 3.7 нм⁻¹ по z при H = 3 и 1.0 нм⁻¹ по z при H = 11.
2. Соглашение о α в коде такое: реальная часть erfc(√α·r)/r, обратная часть exp(−k²/4α). При α = sqrt(−ln eps)/r_c = 2.88 нм⁻² получается erfc(√α·r_c) ≈ 3.4e-4, то есть точность реальной части около 1e-4 по порядку, а не 1e-8. Это тоже надо оценить отдельно.
3. Предварительная проверка независимым numpy-скриптом (пара зарядов +1/−1 в слэбе 20 × 20 × H, энергия относительно разнесённой пары, продакшн сетка против сходящейся):

   | Пара | H = 3 | H = 11 |
   |---|---|---|
   | вертикально 0.33 нм (ион над зарядом стенки) | −2.65 против −2.93 (−9%) | −2.30 против −2.91 (−21%) |
   | латерально 0.66 нм | −1.38 против −1.41 | −1.04 против −1.40 (−25%) |
   | латерально 2.0 нм | −0.34 против −0.40 | −0.33 против −0.39 |

   Единицы e²/(4πε₀ε·нм). 1 единица ≈ 0.72 kT при ε = 78 и T = 298 K.
4. Косвенные признаки в данных. В `disjoining_pressure_contact.csv` (ноутбук `disjoining_pressure_contact.ipynb`) видно следующее.
   - Контактная плотность Na падает с 7.1 нм⁻³ при H = 3 до 4.0 нм⁻³ при H = 11. Для размазанного заряда по контактной теореме она должна быть почти постоянной и не меньше σ²/(2ε₀ε·kT) ≈ 6.6 нм⁻³.
   - Баланс kT·Σ nᵢ(контакт) − σ²/(2ε₀ε) ≈ kT·Σ n(центр) выполняется при H = 3 (в пределах ±1.5 МПа) и нарушается на 4–11 МПа при H ≥ 5.
   - Логируемая `solvation_force` (кулоновская сила на атомах стенки, `Topology.compute_solvation_force`) в нейтральных расчётах составляет 0.55 от σ²/(2ε₀ε) при H = 3 и 0.34 при H = 11. По теореме Гаусса для размазанного заряда должно быть ≈ 1. В ненейтральной серии `results_prod` (без противоионов) та же величина совпадала с σ²/(2ε₀ε) лучше 1%. Возможно, силы на статических атомах стенки обновляются инкрементально (`force_recompute: false`) и устаревают, а возможно, это следствие п. 1. Надо разобраться.

## Шаги

### 1. Инвентаризация параметров

Для **каждого** расчёта в `results_prod_neutral/**/` (включая новые из свипа C_z, если они уже стартовали) прочитай `config_backup.yaml` и собери таблицу: pz, H, σ, C, Cz, L_x, L_y, L_z·z_scale, число атомов, eps, real_cut, alpha, n_c, dipole_correction, dielectric, фактический k_max по каждой оси и требуемый. Проверь, что параметры Эвальда во всех расчётах одинаковые. Отдельно отметь, есть ли расхождения с тем, что описано выше. Для справки сделай ту же таблицу для `results_prod` (ненейтральная серия, в статью не идёт).

### 2. Разобраться в реализации

Прочитай `src/ewald_handler.py`, `src/forces/ewald*.py`, `src/topology.py` (`compute_energy_forces`, `get_energy_difference_translation`, `compute_solvation_force`, `recompute_caches`), `src/sampler.py`. Коротко зафиксируй в отчёте:
- соглашения (α, self-term, нормировка V с z_scale, поправка M_z² − Q_t·G_z, как обрабатываются образы в реальной части, где отсекается r_c);
- как считается ΔE для MC шага трансляции и поворота (инкрементально через структурный фактор или полностью);
- как и когда обновляются силы на статических атомах стенки и откуда берётся `solvation_force` в логе.

### 3. Эталон

Нужен эталон электростатической энергии и сил для конфигураций слэба, заведомо сошедшийся.
- **Вариант А.** Та же схема Ewald3D + slab correction, но в отдельном скрипте в `ewald_audit/` с **анизотропной** сеткой n_x, n_y, n_z = ceil(k_max·L_i/2π). Параметры α и r_c выбери так, чтобы и реальная, и обратная части давали относительную ошибку ≲ 1e-5. Обратную часть считай блоками по k, чтобы не раздувать память, на GPU через CuPy или на CPU.
- **Проверь сходимость эталона.** Увеличь k_max ещё в 1.5 раза и z_scale с 4 до 6 и 8 (для slab correction). Энергия и ΔE не должны меняться в пределах 1e-4 kT.
- **Проверь эталон независимо.** Во-первых, постоянная Маделунга NaCl (1.7476) в 3D. Во-вторых, пара и небольшой набор зарядов в слэбе против прямой суммы по большому числу латеральных образов (или против эталонных значений Ewald2D/Lekner, если удобно).
- Параллельно считай те же величины **продакшн кодом** (через `System`/`Topology`/`EwaldHandler` с параметрами из `config_backup.yaml`), чтобы ловить не только ошибку параметров, но и возможные баги реализации.

### 4. Проверка на реальных конфигурациях

Для каждой группы: базовая точка (H = 3, σ = −20, C = 0.01, C_z = 0.1) для 17 D и, если уже есть, для 27.6 D; каждое H = 5, 7, 9, 11; каждое σ = −5, −10, −15; C = 0.04; C_z = 0; а также самая плотная система C_z = 0.7, если её траектория уже есть. Возьми по 3–5 кадров из уравновешенной части (кадр ≥ 2000, кадры в `trajectory.xyz`, атомы идут в одном и том же порядке). Молекулы (тройки Ip, Im, G) восстанавливаются по порядку атомов. Сверь с `src/system.py`, как они записываются.

Для каждого кадра посчитай следующее.
1. **Полная энергия.** Продакшн против эталона, абсолютно (kT) и относительно.
2. **ΔE для MC шагов. Это главная метрика**, потому что MC использует только разности энергий. Сделай ~300 пробных ходов каждого типа: трансляции Na у стенки (z < 1 нм), Na в объёме, Cl, трансляции и повороты диполей, с амплитудами как в `sampler` (max_displacement 1.0 нм, max_rotation 180°) и маленькими (0.1 нм). Посчитай ΔE продакшн и эталонно. Отчитайся по RMS и максимуму |ΔE_prod − ΔE_ref| в kT, а также по средней разнице вероятностей принятия |min(1, e^{−βΔE_prod}) − min(1, e^{−βΔE_ref})| и доле ходов, где она > 0.1. Ходы, запрещённые жёсткими сферами и стенками, исключай.
3. **Сила на стенку.** Σ F_z по атомам Wb и Wt (на единицу площади): продакшн полным пересчётом, эталон и σ_eff²/(2ε₀ε) для сравнения. Отдельно сравни с логированной `solvation_force` из `simulation_data.csv` для того же кадра (кадр k в xyz соответствует строке k в csv при log_interval = xyz_interval = 50, проверь это по полю step). Если лог расходится с полным пересчётом тем же продакшн кодом, значит логируемая сила устаревает. Найди причину и укажи место в коде.
4. **Раздельно реальная и обратная части**, чтобы понять, какая из ошибок доминирует (недоразрешённая k-сетка, неточная реальная часть или slab correction).

### 5. Проверка на уровне наблюдаемых (если есть свободная GPU)

Самый прямой ответ на вопрос «меняются ли результаты». Запусти **один** короткий расчёт базовой точки (17 D, H = 3, σ = −20, C = 0.01, C_z = 0.1) с исправленными параметрами Эвальда (анизотропная сетка или корректный n_c по максимальной стороне, α и r_c подобраны по стоимости) и, если позволяет время, один при H = 11. Для этого может понадобиться минимальная правка кода (анизотропные n_c). Делай её в отдельной ветке или копии, **не** в рабочем `src/`, который используют идущие задачи. Результаты клади в `results_tmp/ewald_audit/`. Длина около 200–300k шагов, если успевает за разумное время. Сравни со старыми расчётами: профиль Na у стенки, контактную плотность Na, плотности в центре поры, Π = kT(√n₊ − √n₋)² в центре, профиль цвиттерионов. Используй функции из `disjoining_pressure_contact.ipynb` или `prod_visual.ipynb`. Оцени, выходят ли различия за статистическую погрешность.

### 6. Стоимость исправления

Если параметры нужно менять:
- предложи конкретные α, r_c, n_x/n_y/n_z (или эквивалент) для H = 3, 5, 7, 9, 11 с целевой точностью ΔE ≲ 0.01–0.05 kT;
- замерь скорость MC (it/s) с новыми параметрами при нескольких расчётах на одной GPU, как в продакшне (там было ~25 it/s на расчёт при 22 параллельных);
- оцени время одного расчёта на 1e6 шагов и полный объём пересчёта нейтральной сетки (сколько расчётов, сколько узло-часов), с учётом лимита партиции 24 ч и нескольких расчётов на GPU;
- оцени, есть ли дешёвый вариант (например, пересчитать только H = 3 и свип по C_z, а свип по H либо целиком, либо не пересчитывать).

### 7. Вердикт и отчёт

Запиши `ewald_audit/EWALD_AUDIT_REPORT.md`:
- короткое резюме на 5–10 строк вверху: вердикт по каждой группе (H = 3 базовая, свип σ при H = 3, свип C, свип C_z включая идущие 7 расчётов, свип H при H = 5…11) и общий ответ, нужно ли пересчитывать и что именно;
- критерии вердикта. Ориентиры такие: «годится», если RMS ошибки ΔE < 0.05 kT, доля ходов с разницей вероятности принятия > 0.1 меньше 1%, а наблюдаемые из шага 5 совпадают в пределах погрешности; «пересчитывать», если ошибки порядка 0.3 kT и выше или наблюдаемые систематически сдвинуты. Если по данным нужны другие пороги, обоснуй;
- таблицы и графики из шагов 1–5, сделай графики ошибки ΔE в зависимости от H;
- найденные баги с указанием файла и строки (включая `solvation_force`, если подтвердится);
- рекомендации по исправлению кода и конфигов (анизотропные n_c, правильная формула α, выбор L в n_c), предложенные параметры и оценка стоимости из шага 6;
- рекомендацию по идущим 7 расчётам: дать досчитаться или снять.

Все скрипты оставь в `ewald_audit/`, чтобы их можно было перезапустить.
