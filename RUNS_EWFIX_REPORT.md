# Продакшн-запуски с исправленным Ewald

Отчёт по `CLAUDE_TASK_ewfix_production.md`. Разделы 1–3 написаны **до** запуска
продакшна, как требует ТЗ; разделы 4–5 дописываются по ходу и по окончании.

---

## 1. Проверка исправленного кода (этап 1)

Критерий приёмки — RMS ΔE ≤ 1e-3 kT на кадрах реальных траекторий. Измерено на
трёх прогонах по 5000 шагов (H = 3, 5, 11), по дельтам принимаемых движений,
то есть ровно на той величине, которую использует сэмплер:

| H, нм | RMS ΔE, kT | предел |
|------:|-----------:|-------:|
| 3     | 6.7e-5     | 1e-3   |
| 5     | 4.7e-5     | 1e-3   |
| 11    | 6.0e-6     | 1e-3   |

Запас 15–170×.

### 1.1. Остаточная ошибка определяется вакуумным зазором, а не H

Существенный результат аудита: остаточная ошибка Ewald3D+slab зависит только от
величины вакуумного зазора `H·(z_scale − 1)`, а не от самого H. Замеры:

| зазор, нм | RMS ΔE, kT |
|----------:|-----------:|
| 15        | 1.3–1.6e-3 |
| 21        | 1.6–2.0e-4 |
| 25        | 7.5e-5     |
| 27        | 4.1e-5     |
| 33        | 4.9e-6     |
| 35        | 1.4–2.0e-6 |

Примерно декада на каждые 7 нм зазора. Отсюда правило в конфиге:

```yaml
z_scale_factor: ${eval:"max(4.0, (${H} + 25.0) / ${H})"}
```

то есть зазор не меньше 25 нм при малых H и не меньше 3·H при больших:

| H, нм | z_scale | Lz, нм | зазор, нм |
|------:|--------:|-------:|----------:|
| 3     | 9.3333  | 28     | 25        |
| 5     | 6.0     | 30     | 25        |
| 7     | 4.5714  | 32     | 25        |
| 9     | 4.0     | 36     | 27        |
| 11    | 4.0     | 44     | 33        |

При этом α = 0.170562 во всех точках, так что блок `ewald` отличается от старого
`configs_prod/ions_dipole.yaml` только по результатам аудита, как и разрешено ТЗ.

### 1.2. Артефакт перечитывания траектории (не дефект расчётов)

Первый прогон регрессии на H = 3 дал `wall force prod/ref = 0.0000`. Причина
найдена и лежит целиком в файле, а не в расчёте: `trajectory.xyz` пишется с
шестью знаками после запятой. Атом Na, чей z прогон держал легально внутри
слоя, печатается как `2.669000`; при чтении обратно `3.0 − 2.669 =
0.33099999999999996`, что на **один ULP** меньше `r_wall = 0.331`. `HardWall`
возвращает `inf`, `compute_energy_forces` коротко замыкается на нулевой массив
сил для всех атомов — и сила на стенке читается ровно нулём.

Сам прогон был здоров: записанная энергия 3985.36 кДж/моль конечна, логируемый
`solvation_force` = 16.43933 совпадает с эталоном.

Исправлено в двух местах:

* `ewald_audit/scripts/frame_audit.py` — кадр при загрузке подтягивается внутрь
  слоя, но только в пределах точности записи (5e-7 нм); настоящий выход за
  границу по-прежнему падает с ошибкой, а бесконечная энергия кадра теперь
  явно отвергается вместо тихого нуля;
* `src/utils.py::_build_z_limits` — верхняя граница укладки подтягивается к
  последнему float, который стенка действительно принимает, так что интервал
  укладки по построению является подмножеством разрешённого гамильтонианом.

После исправления на тех же кадрах: коррекция 4.44e-16 нм, сила на стенке
16.4433 / 16.4463 против эталона — совпадение.

---

## 2. Конфигурация (этап 2)

`configs_prod/ions_dipole_ewfix.yaml`. Физика и параметры модели совпадают со
старым `configs_prod/ions_dipole.yaml`. Отличаются только:

* блок `ewald` (по результатам аудита, см. 1.1);
* режим стартовой укладки — `random_hs`;
* `results_dir` / имена прогонов и распределение по GPU.

Других отличий нет.

---

## 3. Укладка и бенчмарк (этап 3)

### 3.1. Укладка плотных систем

Проверены четыре самые плотные точки (`ewfix_tests/check_packing.py`, CPU):

| точка | атомов вне слоя | перекрытий | мин. зазор, нм | заряд, e | E₀, кДж/моль | время |
|---|---:|---:|---:|---:|---:|---:|
| H=3, Cz=0.7, pz=17.0 | 0 | 0 | 5.1e-5 | 0.000e+00 | 955.62 | 4.9 с |
| H=3, Cz=0.7, pz=27.6 | 0 | 0 | 8.0e-6 | 0.000e+00 | 1914.15 | 4.8 с |
| H=3, C=0.04          | 0 | 0 | 3.7e-4 | 0.000e+00 | 3968.84 | 2.6 с |
| H=11                 | 0 | 0 | 2.6e-3 | 0.000e+00 | 23429.62 | 4.1 с |

Все начальные энергии конечны, электронейтральность точная, ни одного
перекрытия твёрдых сфер и ни одного атома вне своего z-слоя. Укладка занимает
секунды, а не минуты, так что на общее время расчёта она не влияет.

### 3.2. Кэшированная энергия

Прогон с `sampler.force_recompute: true` снят с плана ради экономии времени по
просьбе автора. Существо проверки покрыто разделом 1: RMS ΔE считается именно
по дельтам движений против полного пересчёта, и запас там 15–170×. Отношение
`solvation_force` к σ_eff²/(2ε₀ε) проверяется на всех 32 прогонах в этапе 5.

### 3.3. Бенчмарк: сколько процессов на одну V100

`ewfix_tests/sbatch_bench.sh`, node211-27, 4× V100, 128 ядер. Скорость берётся
из `simulation_data.csv` по хвосту прогона (первые 40 % отброшены как разогрев),
а не из tqdm.

| H | проц./карту | потоков | it/s на процесс | мин. | ч на 1e6 шагов |
|--:|--:|--:|--:|--:|--:|
| 3 | 1  | не задано | 26.94 | 26.94 | 10.31 |
| 3 | 1  | 1 | 27.23 | 27.23 | 10.20 |
| 3 | 8  | не задано | 26.82 | 26.44 | 10.51 |
| 3 | 8  | 1 | 26.78 | 26.50 | 10.48 |
| 3 | 2  | 1 | 26.77 | 26.69 | 10.41 |
| 3 | 4  | 1 | 26.95 | 26.83 | 10.35 |
| 3 | 16 | 1 | 26.40 | 25.96 | 10.70 |
| 11 | 1 | 1 | 17.51 | 17.51 | 15.86 |
| 11 | 2 | 1 | 17.50 | 17.42 | 15.95 |
| 11 | 4 | 1 | 17.25 | 17.10 | 16.25 |
| 11 | 8 | 1 | 16.84 | 16.77 | 16.56 |

Главный вывод: **скорость на процесс практически не падает до 8 процессов на
карту** — 26.78 против 27.23 it/s при H = 3 (−1.7 %) и 16.84 против 17.51 при
H = 11 (−3.8 %). V100 далека от насыщения: одна MC-цепочка загружает её на
единицы процентов, память — десятки мегабайт. Число потоков BLAS роли не
играет при одном процессе, но при 8 процессах оставлять его незаданным опасно
(каждый процесс считает, что владеет 128 ядрами), поэтому в продакшне жёстко
`OMP_NUM_THREADS=1`.

Оговорка: в строках с `procs` 2 и 16 при `proc/gpu` 1 и 8 агрегатная скорость
была завышена вдвое из-за того, что glob подхватывал каталоги предыдущего
замера той же точки; исправлено (`--expect` в `parse_bench.py`, серийный номер
точки в `sbatch_bench.sh`). На скорость *на процесс* этот дефект не влиял, а
план строится именно по ней.

Не измерено: 4 карты одновременно (32 процесса на 128 ядер). Замер снят по
просьбе автора ради времени; вместо него это проверяется на самих продакшн-
расчётах в первые 15 минут, что этап 4 и так требует. Если скорость окажется
заметно ниже табличной, план пересобирается на `PER_GPU=4`.

### 3.4. Выбранный план упаковки

`ewfix_tests/plan_packing.py` раздаёт 32 прогона по 4 картам из измеренной
модели стоимости шага `t_step = a·N_atoms + b·n_k`, где a = 0.009271 мс/атом,
b = 0.003416 мс/k-вектор.

| карта | прогонов | GPU-часов (если бы каждый прогон был один на карте) |
|--:|--:|--:|
| 0 | 8 | 91.1 |
| 1 | 8 | 91.1 |
| 2 | 8 | 90.7 |
| 3 | 8 | 90.7 |

Итого 363.5 GPU-часов при монопольном владении картой. Балансировка — в
пределах половины процента.

Тяжёлые точки (H ≥ 9 или C_z ≥ 0.7) разведены по разным картам, как требует ТЗ:

| карта | прогон | ч |
|--:|---|--:|
| 0 | `ions_dipole_H_pz_17_H_11_...`   | 15.88 |
| 1 | `ions_dipole_H_pz_27p6_H_11_...` | 15.88 |
| 2 | `ions_dipole_Cz_pz_17_H_3_..._Cz_0.7` | 14.47 |
| 2 | `ions_dipole_H_pz_17_H_9_...`    | 13.90 |
| 3 | `ions_dipole_Cz_pz_27p6_H_3_..._Cz_0.7` | 14.47 |
| 3 | `ions_dipole_H_pz_27p6_H_9_...`  | 13.90 |

По картам: {0: 1, 1: 1, 2: 2, 3: 2}.

**Решение: 8 процессов на карту, все 32 прогона в полёте одновременно.**
Поскольку скорость на процесс при этом почти не падает, стена времени — это
просто самый долгий одиночный прогон: H = 11 при 8 процессах на карту,
**≈ 16.6 ч**. `n_steps = 1e6` не уменьшается.

Запускается это одним slurm-заданием на весь узел: кластер ограничивает
пользователя 20 заданиями в очереди, а 32 отдельных submit туда не помещаются.
Очередь — `AMD7V12-V100-CCP` (не common).

---

## 4. Продакшн (этап 4)

*(заполняется по ходу запуска)*

## 5. Контроль качества и приёмка (этап 5)

*(заполняется по окончании)*
